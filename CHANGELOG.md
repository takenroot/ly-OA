# Changelog

本文件记录 ly-OA 的主要功能变更。完整的提交历史请查看 `git log`。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [1.0.0] - 2026-08-14

### 新增

- 纸质收料单照片整图 OCR 识别，基于印刷 label（编号/车号/毛重/检毛/皮重/检皮/净重）锚定取值。
- 多车间版式支持：通过 `workshops/车间名.json` 配置字段别名与边界，已内置 `示例车间A`、`示例车间B`。
- 每个车间/date 目录分别生成日报：`车间/日期/固废填埋日统计表{MMDD}.xlsx`。
- 统计表标题按数据源目录名动态替换，如“示例车间A”“示例车间B”。
- 识别失败或低置信字段在 Excel 中标黄，备注列带原图超链接，可直接点击核对。
- 照片方向自动校正：优先 0° 识别，不完整或标黄时自动尝试 90°/180°/270° 选最优。
- 图形配置界面（GUI）：设置数据源根目录、开启/关闭定时任务、手动跑昨天/指定日期。
- 命令行模式：`--auto`（跑昨天）、`--day`、 `--input`、`--help`。
- Windows 定时任务：默认每天 08:30 跑前一天，通过 GUI 一键启用/停用。
- 运行完成后 Windows 右下角通知，显示成功/失败/标黄数量。
- 单文件 `ly-oa.exe` 部署，首次运行自动释放 `config.ini`、`template.json`、参考模板、`workshops/` 等文件。
- 按天日志：`logs/ly-oa_{YYYYMMDD}.log`，包含每张图片明细与每次运行汇总。

### 优化

- OCR 引擎限制最多 2 线程，降低 CPU 占用。
- 优先 0° 识别，减少不必要的旋转尝试。
- 隐藏命令行窗口，双击 `ly-oa.exe` 直接进入 GUI。

### 修复

- 装车时间统一为优先从 `检毛` 取、`检皮` 兜底，示例车间A与示例车间B逻辑一致，不再随 `date_field` 变化。
- 修复 `workshops/` 车间版式配置未打入 exe 导致部署后全部识别失败的问题。
- 修复 WPS/Excel 打不开照片超链接的问题。
- 修复合计行记录数统计异常。

### 变更

- 移除独立的“待人工”目录，改为在 Excel 内通过超链接直接核对原图。
- 移除照片文件名兜底日期，改为由用户通过目录名/界面指定日期并人工核验。
- `dist/` 目录不再提交到 GitHub，仅本地保留。

## [0.1.0] - 2026-08-09

### 新增

- 初始版本：Windows 部署支持、exe 打包、任务计划、多数据源合并、运行日志。
